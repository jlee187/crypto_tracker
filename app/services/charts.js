import Ember from 'ember';

export default Ember.Service.extend({
  // removeChart (model) {
  //   return this.get('ajax').del(`/sign-out/${this.get('credentials.id')}`)
  //   .finally(() => this.get('credentials').reset());
  //   <script type="text/javascript" src = "https://widgets.cryptocompare.com/serve/v3/coin/chart?fsym=(model.name)&tsyms=USD,EUR,CNY,GBP">
  //   </script>
  // },
});
